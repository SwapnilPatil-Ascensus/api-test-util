# Synthetic Fixtures

These files contain invented routes and nonfunctional example values used only to prove sanitization, reconciliation, and artifact generation. They do not contain real credentials, member data, hosts, or production information. The `.invalid` hostnames cannot resolve by design.
