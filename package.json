{
  "name": "api-evidence-mapper-local-tools",
  "private": true,
  "version": "0.1.0",
  "description": "Local Postman collection validation and execution tools",
  "scripts": {
    "validate:collection": "node tools/validate_collection.js",
    "newman:version": "newman --version"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "postman-collection": "^5.3.0"
  }
}
