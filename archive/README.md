# Archive

Historical reference only. Not required for normal operation.

The email-safe transport bundle that delivered this repository was consumed during the initial reconstruction on 2026-07-02. Payload blueprints and transport instruction files are not retained in git to avoid bloat.

If you need to reconstruct from an email-safe transport ZIP again, keep the extracted transport folder outside this repository and follow `01-CURSOR-START-PROMPT.txt` from that package.
